const gameBoard = (() => {
  let _board = new Array(9);
})();

const Player = (sign) => {};

// Use minimax algorithm for the logic
const minimaxAiLogic = () => {};

const gameController = (() => {})();
